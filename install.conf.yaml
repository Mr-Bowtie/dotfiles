- defaults: 
    link: 
      relink: true 

- clean: ['~']

- link: 
    ~/.config/fish:
    ~/.config/nvim: 
    ~/.config/kitty:
    ~/.config/git:
    ~/.config/ranger:

- shell: 
    - [git submodule update --init --recursive, Installing submodules]

