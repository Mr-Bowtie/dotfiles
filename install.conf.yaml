- defaults: 
    link: 
      relink: true 

- clean: ['~']

- link: 
    ~/.config/fish:
    ~/.config/nvim: 
    # ~/.config/awesome/rc.lua:
    # ~/.config/aesome/themes/powerarrow: 
    ~/.config/termite/config: 
    ~/.config/git:
    ~/.config/ranger:

- shell: 
    - [git submodule update --init --recursive, Installing submodules]

