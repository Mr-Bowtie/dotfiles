- defaults: 
    link: 
      relink: true 

- clean: ['~']

- link: 
    ~/.config/fish/config.fish:
    ~/.config/nvim: 
    ~/.config/awesome/rc.lua:
    ~/.config/awesome/themes/powerarrow: 
    ~/.config/termite/config: 
    ~/.config/git:
    ~/.config/ranger:

- shell: 
    - [git submodule update --init --recursive, Installing submodules]

